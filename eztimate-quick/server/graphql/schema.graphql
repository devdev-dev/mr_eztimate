type Query {
    getUser(id: ID!): User
}

type User {
    _id: ID!
    name: String
    email: String
    avatar: String
}

type Mutation {
    updateActiveUser(input: UserInput!): User
}

input UserInput {
    avatar: String
    name: String
}